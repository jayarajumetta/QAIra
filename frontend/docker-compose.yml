services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qaira-frontend
    ports:
      - "8080:80"
    environment:
      QAIRA_API_BASE_URL: ${QAIRA_API_BASE_URL:-/api}
